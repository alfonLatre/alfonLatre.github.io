source "https://rubygems.org"

# This mirrors the exact gem versions GitHub Pages builds with, so what
# works locally will also work once you push to GitHub Pages.
gem "github-pages", group: :jekyll_plugins

# Windows/JRuby support (harmless on other platforms).
platforms :mingw, :x64_mingw, :mswin, :jruby do
  gem "tzinfo", ">= 1", "< 3"
  gem "tzinfo-data"
end

